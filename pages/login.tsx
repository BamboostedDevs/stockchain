import React, { Component } from "react";
import { Login } from "../components/Segments";
import Layout from "../components/Layout";

export default class privateRegister extends Component {
  render() {
    return (
      <Layout>
        <Login />
      </Layout>
    );
  }
}
